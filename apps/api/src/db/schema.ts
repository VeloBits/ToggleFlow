/**
 * Drizzle schema for the control plane.
 *
 * Tables land here as features are built: orgs, projects, environments,
 * tools/flags, targeting rules, config versions, API keys, audit log.
 * `pnpm db:generate` emits SQL migrations into ./drizzle (committed).
 */
export {};
